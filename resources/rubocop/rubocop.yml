# project-specific customizations and overrides

inherit_from: default.yml

# the whole project is effectively one big Rakefile, so
# we use the arrow syntax liberally (to declare tasks)
Style/HashSyntax:
  Enabled: false
